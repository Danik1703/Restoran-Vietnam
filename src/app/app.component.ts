import { Component } from '@angular/core';

@Component({
  selector:  'restoran', // 'app-root',
  template:  '<router-outlet></router-outlet>', // <-- також замінюємо стандартний вміст
  })
  
export class AppComponent {
  title = 'restoran';
}
